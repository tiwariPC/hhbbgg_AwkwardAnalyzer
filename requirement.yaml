name: hhbbgg-awk
channels:
  - conda-forge
dependencies:
  - matplotlib==3.8.4
  - uproot==5.3.3
  - hist==2.7.3
  - numpy==1.26.4
  - mplhep==0.3.48
  - vector==1.4.0
  - awkward==2.6.3
  - pandas==2.2.2
  - pyarrow  # Added
  - root  # Added (ROOT may be provided as 'root' or 'root_base' in conda-forge)
  # ML
  - scikit-learn
  - scipy
  - xgboost
  - seaborn==0.13.2
  # Jupyter and related packages
  - jupyter
  - jupyterlab
  - ipykernel
  - ipywidgets
  - nb_conda_kernels
  # PIP packages
  - pip
  - pip:
    - torch
    - torchvision
    - torchaudio  # Match the torchaudio version with torch
    - torch-summary
    - tensorflow
